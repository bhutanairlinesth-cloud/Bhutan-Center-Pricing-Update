# Bhutan Center Pricing System

React + Vite pricing application for Bhutan Center.

## Deploy on Vercel

- Framework preset: Vite
- Build command: `npm run build`
- Output directory: `dist`
- Install command: `npm install`

The current version stores data in browser localStorage. See `SUPABASE_SETUP.md` for the planned shared database setup.
